---
# Collections necessarias. No AWX, use uma Execution Environment que as contenha
# (ou sincronize por este arquivo no Project). O Linux usa so ansible.builtin.
collections:
  - name: ansible.windows
    version: ">=2.0.0"
