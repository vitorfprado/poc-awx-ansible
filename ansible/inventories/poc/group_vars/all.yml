---
# Variaveis GERAIS/FIXAS, comuns ao proxy e ao agent Linux.
# Especificos de ambiente (IP do server, IP do proxy) ficam nos group_vars
# de cada grupo (proxy.yml / linux.yml).

# Versao do Zabbix (controla pacote/repo). Troque AQUI para mudar de versao.
zabbix_version: "7.0"

# Porta padrao do Zabbix Server/Proxy (fixa).
zabbix_server_port: 10051
